import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const Header = () => {
  return (
    <SafeAreaView>
      <Text>헤더</Text>
    </SafeAreaView>
  );
};

export default Header;
